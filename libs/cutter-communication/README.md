# cutter-communication

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cutter-communication` to build the library.

## Running unit tests

Run `nx test cutter-communication` to execute the unit tests via [Jest](https://jestjs.io).
