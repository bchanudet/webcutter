/** Namespace of the plain SVG elements a workspace SVG is built from — see
 * docs/workspace-svg-format.md. */
export const SVG_NS = 'http://www.w3.org/2000/svg';

/** Namespace for the `<webcutter>` export metadata block — see docs/workspace-svg-format.md. */
export const WEBCUTTER_NS = 'https://webcutter.infogones.com/ns/workspace';
